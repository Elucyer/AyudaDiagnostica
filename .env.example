# Backend — configurar en Railway como variables de entorno

# Orígenes CORS permitidos (separados por coma)
# En producción: poner la URL de Vercel
ALLOWED_ORIGINS=http://localhost:3000,https://tu-app.vercel.app

# Dispositivo de inferencia: "cpu" o "cuda"
DEVICE=cpu
